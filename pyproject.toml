[project]
name = "rag-from-scratch"
version = "0.1.0"
description = "A hand-rolled RAG system over personal writing. No frameworks."
requires-python = ">=3.12"
dependencies = [
    "tiktoken>=0.12.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
